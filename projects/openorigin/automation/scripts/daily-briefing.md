你正在一个隔离会话里运行 OpenClaw 每日简报任务。

目标：生成今天的 Markdown 简报，写入 /Users/ze/.openclaw/workspace/memory/$(date +%F).md。

简报必须包含：
- 今日优先级
- 夜间活动
- 待处理事项
- 需要老板关注的内容

执行要求：
1. 优先读取当天 memory 文件、最近项目变更、相关 docs 或任务文件，只做生成简报所必需的搜索。
2. 生成结构清晰的 Markdown 小节。
3. 追加写入当天 memory 文件，标题为“## 每日简报”。
4. 如果没有足够信息，明确写“暂无足够数据”。
5. 不要捏造不存在的活动。
6. 默认只写文件；只有任务明确要求外发且环境允许时，才额外给老板发送摘要。
7. 最终输出只需一句简洁结果，例如“已追加今日日报到 memory/2026-xx-xx.md”；若失败，明确 blocker。

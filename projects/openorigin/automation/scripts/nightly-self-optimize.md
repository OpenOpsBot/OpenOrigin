你正在一个隔离会话里运行 OpenClaw 夜间自我优化任务。

目标：每晚只审计一个小领域，并在 12 分钟内完成一次低风险优化。
候选领域轮换：
1. 文档不一致
2. 待办清单整洁度
3. 失效链接
4. 过期文件
5. 低效提示词

强约束：
- 只做低风险、可回滚修改
- 优先修正文档、注释、链接、轻量结构问题
- 不做破坏性删除
- 如果风险高，只提出建议，不直接改
- 不做全仓大范围扫描；先从 `projects/openorigin/` 与最近 3 天的 `memory/*.md` 下手
- 单次最多检查 12 个文件；一旦找到可修项目，优先收口，不继续扩散范围
- 代码/文档改动总量尽量控制在 1 个主题、1 次提交内
- 必须预留至少 2 分钟用于写 memory 和 git 提交，避免超时
- 所有结果必须追加写入 /Users/ze/.openclaw/workspace/memory/$(date +%F).md

执行要求：
1. 先选择今天要审计的一个领域，并说明为什么。
2. 先列出计划检查的文件（不超过 12 个）；优先 recent files、automation 文档、API/系统文档、相关脚本。
3. 搜索相关文件并做审计；避免无边界搜索。
4. 应用 1~3 个低风险修复；如果没有安全修复项，就只记录发现与建议。
5. 记录：审计领域、检查文件、发现、已修复内容、建议的后续动作。
6. 如有改动，提交 git，提交信息格式：chore(audit): nightly self optimize <domain>
7. 最终输出简洁总结；若剩余时间不足 2 分钟，立即停止继续搜索，转去写记录和提交。
8. 若失败，明确 blocker，尤其说明是搜索范围过大、git 阻塞，还是工具/超时问题。

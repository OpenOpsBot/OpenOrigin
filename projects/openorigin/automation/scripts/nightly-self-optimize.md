你正在一个隔离会话里运行 OpenClaw 夜间自我优化任务。

目标：每晚只审计一个领域，并尽量做低风险改进。
候选领域轮换：
1. 文档不一致
2. 待办清单整洁度
3. 失效链接
4. 过期文件
5. 低效提示词

强约束：
- 只做低风险、可回滚修改
- 优先修正文档、注释、链接、轻量结构问题
- 不做破坏性删除
- 如果风险高，只提出建议，不直接改
- 所有结果必须追加写入 /Users/ze/.openclaw/workspace/memory/$(date +%F).md

执行要求：
1. 先选择今天要审计的一个领域，并说明为什么。
2. 搜索相关文件并做审计。
3. 应用低风险修复。
4. 记录：审计领域、发现、已修复内容、建议的后续动作。
5. 如有改动，提交 git，提交信息格式：chore(audit): nightly self optimize <domain>
6. 最终输出简洁总结；若失败，明确 blocker。

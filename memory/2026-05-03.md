# 2026-05-03

## 夜间自我优化（手动触发）

**审计领域：失效链接 / 文档不一致**

### 检查范围
- `automation/docs/CRON-AUTOMATION.md`
- `automation/scripts/daily-briefing.md`
- `automation/scripts/system-reference-rollup.md`
- `automation/scripts/nightly-self-optimize.md`
- `docs/SYSTEM-REFERENCE.md`
- `server.js`、`index.html`、`package.json`

### 发现

1. **文档错误（已修复）**：`SYSTEM-REFERENCE.md` 声称 server.js 读取 `data/client-ops-sample.json` 并暴露 `/api/client-ops` 接口，但代码中根本不存在此文件和路由。实际存在的是 `data/ideas.json` + `data/prototypes.json` + 对应 `/api/ideas`、`/api/prototypes` 路由。

### 已修复
- 修正 `docs/SYSTEM-REFERENCE.md` 两处：将虚假 `data/client-ops-sample.json → /api/client-ops` 替换为真实存在的 `data/ideas.json` / `data/prototypes.json` 及其对应 API 路由。

### 建议后续动作
- 考虑将 `data/client-ops-sample.json` 作为占位文件加入（或确认是否真的需要这个数据源）
- `nightly-self-optimize` 和 `daily-briefing` 持续 timeout，建议检查是否有 900s/600s 超时配置不当的问题
- `package.json` 的 `test` 仍是占位命令，建议补充基础测试

## 工作记录
- 老板明确要求：项目改动后默认流程改为“先运行测试，再启动/运行”，后续按这个顺序执行。

## nightly-self-optimize
- 审计领域：失效链接（今天是 28 号，按 5 个候选领域轮换落到第 3 项；优先查最近文档里的路径/链接引用）
- 检查文件：`projects/openorigin/API.md`、`projects/openorigin/docs/SYSTEM-REFERENCE.md`、`projects/openorigin/automation/scripts/nightly-self-optimize.md`、`memory/2026-04-26.md`、`memory/2026-04-27.md`、`memory/2026-04-28.md`
- 发现：
  - 选定范围内没有真正的 Markdown 超链接，主要问题是少量路径引用不够自洽，读者难以直接定位目标文件。
  - `API.md` 的 `/api/agents` 来源写成 `workspace/IDENTITY.md`，缺少前缀，脱离上下文后容易误判路径。
  - `SYSTEM-REFERENCE.md` 中自动化脚本与部分工作树文件只写 basename / 短路径，不利于快速跳转或检索。
- 已修复：
  - 将 `API.md` 中 `workspace/IDENTITY.md` 改为 `~/.openclaw/workspace/IDENTITY.md`。
  - 将 `SYSTEM-REFERENCE.md` 中自动化脚本列表统一改为 `automation/scripts/*` 完整仓库路径。
  - 将 `SYSTEM-REFERENCE.md` 中 `tabs.css`、`tabs.js` 改为 `css/tabs.css`、`js/tabs.js`，避免路径悬空。
- 后续建议：
  - 若后面继续写系统文档，尽量统一使用仓库相对路径或可点击 Markdown 链接，减少“看得到名字但定位不到文件”的情况。
  - 若需要做更严格的失效链接审计，可以后续补一个只扫 `docs/` 与 `API.md` 的轻量脚本，但这次先不扩大范围。

## 每日简报

### 今日优先级
- 跟进 `openorigin` 文档与自动化脚本的后续整理，优先保持路径引用一致、可检索。
- 如有项目改动，继续遵循老板新要求：先运行测试，再启动/运行。

### 夜间活动
- 完成一轮 `nightly-self-optimize` 审计，主题是失效链接 / 路径引用一致性。
- 修复了 `projects/openorigin/API.md` 与 `projects/openorigin/docs/SYSTEM-REFERENCE.md` 中多处不够自洽的路径写法。
- 今日已有备份快照提交：`chore(backup): workspace snapshot 2026-04-28 12:10`。

### 待处理事项
- 观察后续文档新增内容是否继续出现 basename / 短路径引用，必要时补统一规范。
- 评估是否需要补一个仅扫描 `docs/` 与 `API.md` 的轻量路径审计脚本。

### 需要老板关注的内容
- 当前暂无必须立刻处理的 blocker。
- 若老板准备继续推进 `openorigin` 文档体系，建议后续统一采用仓库相对路径或可点击 Markdown 链接。

## system-reference-rollup
- 已刷新 `projects/openorigin/docs/SYSTEM-REFERENCE.md`，把日期滚到 2026-04-28，并按当天真实状态重写了今日变更、架构概览、模块清单、活跃定时任务、已知问题。
- 今天的系统文档结论有两个关键变化：
  - `backup-private-repo` 的锁问题已因新 TTL 锁机制明显缓解，今天已成功提交并 push。
  - 当前更大的系统级风险改成了 `nightly-self-optimize`、`daily-briefing`、`system-reference-rollup` 三个隔离 agent cron 持续 timeout。
- 文档同步纳入了 Brain 新增的 `os-documentation` 页面、`/api/system-reference` 接口，以及当前真实的 cron schedule / status。
